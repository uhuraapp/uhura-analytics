# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  subject: "Hello <%%= user.email %>"
  body: "Hello <%%= user.email %>, are you user #<%%= user.id %>? Welcome from <%%= letter.id %> letter"
  done: true

two:
  subject: MyString
  body: MyText
  done: false
